# 工程交接紀錄

> **規則：換帳號後、開新 session、或開工，不管哪一種情況，第一件事永遠是先讀完這份文件最上面（最新）的那一筆紀錄，再開始工作。**
> 每次工作告一段落、要換帳號或結束 session 前，依照下面的格式在「紀錄」區塊最上面新增一筆（最新的放最上面）。

## 紀錄格式

每筆紀錄固定包含這些欄位，缺項目就寫「無」，不要整節刪掉：

1. **目前架構** — 這次 session 當下，整個專案的技術架構、模組、環境（給完全沒背景的人也能看懂現況）
2. **已完成項目** — 這次 session 做完、驗證過的事
3. **正在做什麼 / 尚未完成** — 做到一半、還沒收尾的事
4. **修改過的部份** — 有異動的檔案清單（新增/修改）
5. **下一步** — 接下來建議先做什麼
6. **已知問題** — 目前還沒解決、或使用者已知悉先擱置的問題
7. **公式異動** — ⚠️ 如果這次 session 有修改任何計算公式（例如裝載試算、SLA 天數、排序邏輯等含運算的邏輯），**必須在這裡特別列出改了什麼、改前改後差異**；完全沒有異動就寫「無」

---

## 紀錄

### 2026-08-21

**目前架構**
- Next.js 16 (App Router) + Supabase（postgres，前端用 anon key 直連，`lib/supabase.ts`）；帳號系統自建（`employees` + bcrypt，`proxy.ts` 擋 `/admin/*`，權限存 localStorage、登入時才會抓最新的 `permission_group_functions`，所以**每次新授權模組後都要登出重登入**才會出現在側邊欄）
- ⚠️ 本機 `.env.local` 與 Vercel Production（`truck-seven-phi.vercel.app`）是兩個不同 Supabase 專案，資料不同步，schema 異動要兩邊都跑
- 後台模組（`components/admin/AdminLayout.tsx` 側邊欄順序）：Dashboard、Employees、**Role Titles（新增）**、Permission Groups、Vehicle Type、Truck Owners、Trucks、Dispatch Records、Daily Inspections、**Personnel Readiness（原 Driver Readiness，本次擴充改名）**、Improvement Progress、**Accidents（新增）**、**Customer Complaints（新增）**、**Fuel & Cost（新增）**、**KPI Dashboard（新增）**、Inspection Settings、Carton Types、Load Calculator
- 司機端頁面：`/`、`/inspection/new`、`/inspection/[id]`、`/dispatches`、`/improvement`、`/improvement/[id]`、**`/accident`、`/accident/new`、`/accident/[id]`（新增）**
- 新增外部整合：Gemini API（`GEMINI_API_KEY`，server-only，`lib/inspectionAiReview.ts`）、Vercel Cron（`vercel.json`）、`docx` npm 套件（產生 Word 文件）

**已完成項目**（這次 session 內容非常多，起因是使用者貼了一份 ChatGPT 建議的出貨部門 KPI 架構，先產出分析文件跟使用者對齊方向，確認後一次做完；以下照主題分組，不照時間順序）

*1. 既有頁面加篩選/排序/欄位*
- `/admin/trucks`：Owner 下拉 + 車牌搜尋、`registration_expiry`/`insurance_expiry` 欄位
- `/admin/inspections`：部門/Truck Owner/結果下拉 + 日期區間 + 文字搜尋，拆成 All Inspections / Defect Items 兩個 tab
- `/admin/truck-owners`：`is_default`（預設車隊）勾選
- `/admin/employees`：排序、`license_no`/`license_expiry`、「Maintenance Responsible For」多選（`truck_maintenance_assignments`）、`role_level`（1–4，1 最高只能一人，DB unique index + 前端雙重防呆）、Role Level 旁邊的唯讀 Job Title（查表 `role_level_titles`，同一行顯示）
- `/admin/truck-types` 改名「Vehicle Type」（側邊欄 + 頁面本身）
- Dashboard 重做：年份+12月選單、10 格 KPI（Total/Ready/Inspection Due/Defective/Under Maintenance/Critical Issues 即時；Registration/Insurance Expiring/Driver Issues/Inspection Compliance 隨年月變動）、只算 `is_default` 預設車隊、拆成兩個卡片群組
- `/admin/dispatches`：新增 `helper_id`（幫手，跟司機一樣每趟指派）、`scheduled_departure_time`/`scheduled_arrival_time`/`delay_reason`、On-Time 判定欄位；司機端 `/dispatches` 出車時可選延誤原因、顯示排定時間跟幫手

*2. Improvement Progress 擴充*
- 照片欄位、只顯示預設車隊、Truck Owner/Truck Type 篩選+顯示欄、排序、Inspection Date（存成實體欄位 `improvement_actions.inspection_date`，不即時 join）、編號欄、Export Excel
- Repair 欄位改成下拉（Truck Owner／Repair Shop）+ 文字框（`repair_vendor_type`）
- `improvement_actions` 新增 `repair_cost`

*3. AI 嚴重度建議（Gemini）*
- Inspection Settings 每個項目可設 `default_severity`，「✨ AI Suggest」呼叫 Gemini 建議（`app/api/ai/classify-severity`）；已對現有項目跑過一次並回填既有缺失的 `severity`
- 「AI Review Log」（Inspection Settings 頁面下方）+ 每月自動執行（Vercel Cron）：覆核嚴重度建議跟現況是否不同、比對每台車檢查清單跟上次執行的差異，寫進 `inspection_ai_reviews`（純報告，不自動改設定）

*4. Accidents 模組（獨立於 Improvement Progress，交通事故專用）*
- 司機端一次性回報（`/accident/new`：Truck Owner 下拉，預設值＝打勾為預設的 owner，選了會篩選 Truck 清單；到場安全檢查清單；照片上傳）+ 唯讀列表/詳情（`/accident`、`/accident/[id]`）
- 後台分類/調查/結案（`/admin/accidents`、`/admin/accidents/[id]`）：L1–L4 嚴重度、回報時效（發生到通報的分鐘數，依 ≤15/16–30/31–60/>60 分鐘上色）
- **匯出 Word**（`/api/accidents/[id]/export-word`，用 `docx` 套件產生正式事故報告文件，含簽名欄，司機端跟後台都有下載按鈕）

*5. Customer Complaints 模組*
- `/admin/customer-complaints`：純後台 CRUD，kf-erp 沒有這塊資料所以全新建，非 driver-facing

*6. Fuel & Cost 模組*
- `/admin/fuel-costs`：上傳 Shell 加油卡月結單 Excel，前端用 `xlsx` 解析（已用使用者提供的真實檔案 `2026Shell.xlsx` 驗證過欄位對應），車牌正規化比對（去空格、大寫），`receipt_number` unique + upsert ignoreDuplicates 讓重複上傳同一份月結單安全不重複，比對不到車輛的車牌會列出來要求手動指派
- 已知限制：Km 欄位不是每筆都有記錄，km/liter 油耗效率沒做，先只做總量/總金額

*7. Personnel Readiness Check（原 Driver Readiness，本次整個重做）*
- 從「只能檢查司機、5 個寫死欄位」改成「涵蓋任何在職員工（含幫手）、檢查項目可設定」
- 新表 `personnel_check_items`（可在頁面上的「⚙ Check Items」新增/編輯/停用/刪除）+ `driver_readiness_check_results`（每次檢查對每個項目的 Pass/Fail，snapshot 項目文字，不受之後項目改名影響）
- 已預先建立 10 項檢查：Alcohol Test、Mental/Alertness State、Physical/Medical Condition、Fatigue Level、Medication Check、PPE Worn、License/Certification Valid、Uniform & Appearance、Communication Device、Route/Task Briefing
- 首頁新增「Personnel Readiness Check」卡片（僅後台使用者看得到，同 Settings 卡片邏輯）
- 側邊欄改名「Personnel Readiness」

*8. Role Titles（新模組）*
- 新表 `role_level_titles`：Role Level 1–4 對應職稱，預設 1=Supervisor、2=Staff、3=Driver、4=Helper，`/admin/role-titles` 可編輯

*9. KPI Dashboard（`/admin/kpi`，獨立頁面）*
- 年月選單 + 依 `role_level` 篩選
- **車輛 Vehicle Score**（0–100，綁定 `truck_maintenance_assignments` 負責人：100=名下車輛都正常、50=有車在維修中、0=有車故障未處理）
- **紀律 Discipline**（Daily Inspection + Personnel Readiness 雙表完成率平均）
- **安全**：Personnel Readiness 通過率、事故次數/嚴重度
- **準時 On-Time**（依 `dispatches.scheduled_departure_time` 比對實際出車時間，遲到 ≤15 分算準時，`delay_reason` 屬於天氣/客戶改期/封路/工廠延遲可視為準時已排除）
- **成本**：油費（依司機/保養負責人粗略分攤，非精確）、維修費
- **客戶**：客訴數
- Delivery Accuracy 仍留白（kf-erp 待確認）

*10. Bug 修復：RLS 反覆被自動重新啟用*
這個專案這次 session 總共踩到 **4 次**（`016`、`023` 兩支 reassert migration），對象是每次新建的資料表（`improvement_actions`、`truck_maintenance_assignments`、`driver_readiness_checks`、後來的 `accident_reports`/`customer_complaints`/`fuel_transactions`）。已知這是第 4～7 次遇到同類狀況（前面 `006`/`008`/`012` 也是），**根本原因仍未查出**，懷疑跟 Supabase Dashboard 的自動行為有關。重點教訓：**光用 SELECT 測試看不出來**（RLS 擋掉的 SELECT 只會回傳空陣列、不會報錯），一定要實際測 INSERT 才會看到 `42501` 錯誤。之後每次新增資料表，驗證步驟都要包含一次真的寫入測試。

**正在做什麼 / 尚未完成**
- 使用者今天稍早已陸續在**本機** Supabase 執行完 `013`～`026`（過程中抓到 `021`、`022` 各有一次沒跑完整，已個別補上並驗證）
- **Production Supabase 專案完全還沒跑過任何一支這次 session 的 migration**（`013`～`026`），使用者今天要求把程式碼推上 GitHub 並部署上線 —— 這代表 Production 網站接下來會用新版程式碼、但資料庫還是舊的 schema，新模組在 Production 上會直接壞掉（缺欄位/缺表）直到使用者在 Production 的 Supabase SQL Editor 也跑完 `013`～`026`
- 因為多數新功能要登入才能操作，驗證方式主要是：`npx tsc --noEmit` 全過、dev server 無編譯錯誤、用 anon key 直接 curl Supabase REST API 驗證讀寫（含這次抓到的 RLS 問題）、Fuel & Cost 的匯入邏輯有額外用 Node 對真實 Shell 檔案跑過一次
- Fuel & Cost 匯入的月結單裡有 4 個車牌（`NEB8286`、`NKS9268`、`NKT7923`，`W00612`/`WO0612` 疑似同一台車兩種寫法）目前 Trucks 表沒有對應車輛，匯入後會列出來要求手動指派

**修改過的部份**
- 新增 migrations `015`～`026`（12 支，檔名見 `supabase/migrations/`）
- 新增頁面：`app/admin/driver-readiness`、`app/admin/accidents`（含 `[id]`）、`app/admin/customer-complaints`、`app/admin/fuel-costs`、`app/admin/kpi`、`app/admin/role-titles`、`app/accident`（含 `new`、`[id]`）
- 新增 API route：`app/api/ai/classify-severity`、`app/api/ai/monthly-review`、`app/api/cron/monthly-inspection-review`、`app/api/accidents/[id]/export-word`
- 新增 lib：`lib/inspectionAiReview.ts`
- 新增設定：`vercel.json`
- 新增套件：`docx`（Word 文件產生）
- 修改：`app/admin/trucks`、`app/admin/inspections`、`app/admin/truck-owners`、`app/admin/employees`、`app/admin/page.tsx`、`app/admin/improvement-progress`、`app/admin/inspection-settings`、`app/admin/dispatches`、`app/admin/truck-types`、`app/improvement/[id]`、`app/inspection/new`、`app/dispatches`、`app/page.tsx`、`components/admin/AdminLayout.tsx`、`.env.local.example`

**下一步**
1. **使用者要在 Production Supabase 的 SQL Editor 依序執行 `013`～`026`**（最優先，不然部署後 Production 網站會壞）
2. Production 跑完後，實際登入 Production 網站測一輪：Role Level/Job Title、Helper 指派、真的上傳一次 Shell 月結單、跑一次 Accident 回報+匯出 Word、Personnel Readiness Check 設定項目、KPI Dashboard 挑一個月看數字
3. 確認 kf-erp 有沒有客戶/訂單/簽收資料可以接，決定 Delivery Accuracy 怎麼做
4. RLS 重複被打開的根本原因還沒查，之後每次新增資料表都要記得先測 INSERT 再收工

**已知問題**
- 沿用之前紀錄的既有問題（RLS 全開的資安缺口、Truck Owners 裡 4 筆不明資料、inspection-photos bucket 全開放、本機與 Production 是兩個不同專案）
- RLS 會不明原因重複被打開（見上「已完成項目 10」），根本原因未查
- Fuel & Cost 的 km/liter 油耗效率沒有做（Km 欄位不是每筆都有記錄）
- KPI Dashboard 的油費分攤是粗略估算、不是精確拆分；Delivery Accuracy 沒有資料來源
- **Production 資料庫 schema 落後於這次部署的程式碼**（見上「正在做什麼」），部署後到補跑 migration 前，Production 上的新模組會出現讀寫錯誤

**公式異動**
無修改既有公式。新增的計算邏輯（全部是全新功能，不是改既有的）：
- Accident Report Latency 分級（≤15/16–30/31–60/>60 分鐘）
- Dispatch On-Time 判定（遲到 ≤15 分鐘算準時，特定 `delay_reason` 視為準時已排除）
- KPI Dashboard 各項指標計算方式（見上「已完成項目 9」）
Load Calculator 公式沒有觸碰。

---

### 2026-08-18

**目前架構**
- Next.js 16 (App Router) + Supabase（postgres，前端用 anon key 直連，`lib/supabase.ts`）
- ⚠️ 兩個不同的 Supabase 專案：本機 `.env.local` 指向專案 A；Vercel Production（truck-seven-phi.vercel.app）指向專案 B，兩邊資料**不同步**。之後改 schema / 資料，兩邊都要各自處理，不能只改一邊。
- 帳號系統是自建的（不是 Supabase Auth）：`employees` 表 + bcrypt `password_hash`，登入走 `/api/auth/login`，權限走 `permission_groups` / `permission_group_functions`，session 存 localStorage，`proxy.ts` 只用一個 marker cookie 擋 `/admin/*` 進頁面
- 模組：Employees、Permission Groups、Truck Types、Truck Owners、Trucks、Dispatch Records、Daily Inspections、Inspection Settings、Carton Types、Load Calculator、**Improvement Progress（本次新增）**
- 司機端頁面：`/`、`/inspection/new`、`/inspection/[id]`、`/dispatches`、`/improvement`、`/improvement/[id]`
- 後台頁面：`/admin/*`

**已完成項目**
1. 重設 Production 的 `admin` 帳號密碼（過程中發現本機與 Production 是不同 Supabase 專案，改用 Production 自己的 `/api/auth/hash` 產生 bcrypt hash，交給使用者在 Production 的 Supabase SQL Editor 執行 UPDATE）
2. Truck Owners 整理：新增 owner「Logistic Company」；ABD5130 / DBI 5170 / DCB 3104 / ABD5110 / WIL 321 / NDR8770 這 6 台維持 Koufu，其餘 23 台改成 Logistic Company（MRA TRUCKING 那台維持原本的 MRA Trucking 不動）
3. 新增「Improvement Progress」缺失改善追蹤功能（討論定案：兩階段輸入 + SLA 倒數 + 驗收簽核，四階段流程：問題回報 → 判定指派 → 維修回報 → 驗收簽核）
   - `supabase/migrations/013_improvement_actions.sql`：新表 `improvement_actions`、storage bucket `inspection-photos`、`permission_group_functions` 新增 `improvement_progress` function code
   - `supabase/migrations/014_backfill_improvement_actions.sql`：把功能上線前既有的 issue 補建對應的 `improvement_actions`
   - 每日檢查表（`/inspection/new`）：ISSUE 項目可以拍照上傳；header 加「Back to Home」；送出時自動為每個 issue 建立一筆 `improvement_actions`
   - `/improvement`（司機端清單，Open/All 篩選）、`/improvement/[id]`（四階段表單，含嚴重度分級按鈕、SLA 倒數 pill、各階段各自 Save）、`/admin/improvement-progress`（後台清單 + 篩選）
   - 首頁 Improvement Progress 卡片顯示「未結案數」badge
4. Trucks 後台列表（`/admin/trucks`）：欄位標題可點擊排序，再點一次反向排序
5. Dashboard（`/admin`）：新增「Koufu Trucks — Open Defects」卡片，列出每台 Koufu 車尚未解決的缺失（車號 + 缺失內容 + 未結案數）
6. 已 commit + push 兩批：`a5bdb84`（Improvement Progress 全功能）、`3d9fcbe`（Trucks 排序 + Dashboard 缺失卡片 + 首頁未結案數）

**正在做什麼 / 尚未完成**
- migration `013`、`014` 使用者**還沒在 Production Supabase 執行**（只存在於程式碼庫裡）——Improvement Progress 跟拍照上傳實際要能動之前必須先跑
- 因為 migration 還沒跑，**還沒做過端對端實測**（沒有真的登入 Production 走一次「拍照回報缺失 → 後台/首頁看到未結案 → 指派/維修/驗收 → 結案」）

**修改過的部份**
- 修改：`app/inspection/new/page.tsx`、`app/page.tsx`、`components/admin/AdminLayout.tsx`、`app/admin/page.tsx`、`app/admin/trucks/page.tsx`
- 新增：`app/improvement/page.tsx`、`app/improvement/[id]/page.tsx`、`app/admin/improvement-progress/page.tsx`
- 新增：`supabase/migrations/013_improvement_actions.sql`、`supabase/migrations/014_backfill_improvement_actions.sql`

**下一步**
1. 使用者在 Production Supabase 的 SQL Editor 依序執行 `013`、`014`
2. 實測整條 Improvement Progress 流程（見上）
3. 之前討論過、使用者還沒決定要不要做的加強項目：維修費用欄位、嚴重度=緊急時自動限制該車派工、驗收「不合格退回」後要不要通知原負責人
4. 資安缺口（見下）什麼時候要處理，由使用者決定

**已知問題**
1. **【資安，尚未修復】** 專案幾乎所有資料表都 `disable row level security`，且 anon key 是 `NEXT_PUBLIC_` 會被打包進瀏覽器 JS，任何人都能繞過登入直接對 Supabase REST API 讀寫幾乎所有資料（包含 `employees.password_hash`）。已經跟使用者說明風險，使用者選擇先做功能、資安之後再處理，**不要自己動手改 RLS**，要改之前務必先跟使用者確認範圍（牽動所有頁面的資料存取方式）。
2. Truck Owners 清單裡有 4 筆不明來源的資料（AG1 TRUCKING SERVICES、DELAZAR TRUCKING、JHAMAL TRUCKING SERVICES、KOUFU PACKAGING），已確認不是使用者自己加的，目前沒有任何車在用，先保留沒刪，很可能跟上面資安缺口有關（外部寫入探測）。
3. Improvement Progress 的 storage bucket（`inspection-photos`）是 public + anon 可寫，跟現有資料表同一套「全開放」邏輯（見問題1），還沒做檔案類型/大小限制。
4. 本機 `.env.local` 與 Production 是兩個不同 Supabase 專案，這件事很容易忘記，任何 schema 或資料異動都要提醒自己兩邊都處理。

**公式異動**
無。本次沒有修改任何計算公式；Load Calculator（貨車裝載試算）的公式完全沒有觸碰，該工具的公式規格請見 `貨車裝載試算-專案交接文件.md`。

---

<!-- 往上新增下一筆紀錄，最新的永遠放最上面（緊接在「## 紀錄」標題下方） -->
