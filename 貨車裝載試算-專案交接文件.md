# 貨車裝載試算工具 — 專案交接文件

版本：v1（單檔 HTML 原型，直連 Supabase REST API）
用途：本文件說明目前原型的資料結構、核心計算邏輯、三大功能模組規格、Excel 匯出規格與儲存機制，供內部重新開發成正式版本（例如 Next.js + 後端 API + 正式資料庫）時參考。

---

## 1. 專案目標

給定「貨車車廂尺寸」與「包裝箱尺寸」，回答三個問題：

1. **單一產品容量**：某台貨車最多可以裝多少個某種包裝箱？
2. **混裝裝載確認**：某台貨車要裝好幾種不同產品、各自指定數量，這樣裝得下嗎？
3. **貨車推薦**：我有好幾種產品、各自指定數量，公司裡哪幾台貨車裝得下？裝得下的裡面哪一台最剛好（不會太浪費空間）？

---

## 2. 資料來源

### 2.1 貨車資料表 `zz_car_master`

| 欄位 | 說明 |
|---|---|
| `car_number` | 貨車車號（顯示用識別碼） |
| `long_n` | 車廂內部長度 |
| `wide_n` | 車廂內部寬度 |
| `high_n` | 車廂內部高度 |

### 2.2 包裝箱資料表 `zz_pack_box_size_master`

| 欄位 | 說明 |
|---|---|
| `box_name_id` | 包裝箱名稱／識別碼 |
| `box_width` | 箱子寬度 |
| `box_long` | 箱子長度 |
| `box_height` | 箱子高度 |
| `packing_qty` | 每一箱裝多少「件」數量（例如一箱 1000pcs） |

### 2.3 資料過濾規則（重要）

讀取資料後，**必須過濾掉尺寸不完整的資料列**，否則後續計算會出現 `0` 或 `NaN` 之類的誤導結果：

- 貨車：`long_n`、`wide_n`、`high_n` 必須「都存在且 > 0」，否則整台車直接從清單中剔除，不顯示在任何下拉選單或推薦結果裡。
- 包裝箱：`box_width`、`box_long`、`box_height` 必須「都存在且 > 0」，否則整筆箱型剔除。
- `packing_qty` 若缺值或 ≤ 0，視為 `1`（即「這個欄位沒填就當作一箱等於一件」），不因此剔除整筆箱型資料（因為長寬高才是放不放得下的關鍵，`packing_qty` 只影響「總數量」的顯示）。

正式版建議：這個過濾邏輯應該放在後端 API／資料庫查詢層（例如 SQL `WHERE long_n > 0 AND wide_n > 0 AND high_n > 0`），而不是前端過濾，避免把不完整資料傳到前端又浪費頻寬。

---

## 3. 核心假設（所有計算的共同前提）

1. **水平方向（長 × 寬）可以互相旋轉**：箱子放置時，`box_width`／`box_long` 這兩個水平尺寸可以對調方向去對齊車廂的 `wide_n`／`long_n`，取排列數量較多的那個方向。
2. **垂直方向（高度）不可平放、不可旋轉**：`box_height` 只能對齊車廂的 `high_n`，不會把箱子橫放。
3. 所有排列都是「整齊網格式」堆放（不做斜放、交錯堆疊等進階排版）。

---

## 4. 核心計算公式

### 4.1 單一箱型・最佳水平方向

給定車廂 `W = wide_n`、`L = long_n`，箱子 `bw = box_width`、`bl = box_long`：

```
方向 A（不旋轉）：
  rowsA = floor(W / bw)
  colsA = floor(L / bl)
  countA = rowsA × colsA

方向 B（水平旋轉 90 度）：
  rowsB = floor(W / bl)
  colsB = floor(L / bw)
  countB = rowsB × colsB

取 countA 與 countB 中較大者，作為「每層可放幾箱」(footprint)
連帶記錄對應的 footW / footL（該方向下箱子實際佔用的寬與長）
```

### 4.2 堆疊層數

```
layers = floor(H / box_height)      // H = high_n，不可旋轉
```

### 4.3 單一產品・總容量（功能 01 使用）

```
footprint = max(countA, countB)
total_boxes = footprint × layers
total_units = total_boxes × packing_qty     // packing_qty 缺值時視為 1
```

**放不下的判斷（必須明確區分原因，不要只顯示 0）：**

- 若 `footprint <= 0` → 「箱子長寬放不進車廂地板（兩種方向都排不下）」
- 若 `layers <= 0` → 「箱子高度超過車廂高度，無法堆疊」

只要任一條件成立，就不是「總數量 0」，而是要顯示明確的「放不下」訊息與原因，並且不繪製排列圖。

### 4.4 混裝裝載可行性檢查（功能 02、03 共用邏輯）

這是**樓地板面積估算法**，不是精確的二維／三維最佳排版演算法（多箱型混合排版屬於 NP-hard 問題，正式版如果要更精準，需另外評估專門的 bin-packing 演算法或委外排版工具）。

給定車廂與一組「產品＋需求數量」清單 `items = [{box, qty}, ...]`（此處 `qty` 是**箱數**，不是件數）：

```
totalArea = W × L   // 車廂地板總面積

對每一項產品 item：
  packingQty = item.box.packing_qty（缺值視為 1）
  unitQty = item.qty × packingQty                 // 這項產品的總件數

  取得該箱型最佳水平方向 footW / footL（同 4.1）
  layers = floor(H / box_height)

  若兩種水平方向都排不下 → possible = false，原因：「箱子長寬放不進車廂地板」
  否則若 layers <= 0        → possible = false，原因：「箱高超過車廂高度」
  否則：
    columnsNeeded = ceil(item.qty / layers)        // 需要幾根「整柱堆到頂」的柱子
    areaNeeded = columnsNeeded × footW × footL      // 這項產品佔用的地板面積
    累加 usedArea += areaNeeded

fits = (所有 item 都 possible) AND (usedArea <= totalArea)
ratio = usedArea / totalArea   // 地板利用率，用來排序/顯示百分比
totalBoxes = Σ item.qty
totalUnits = Σ unitQty
```

**設計邏輯說明**：每種產品各自被當成一個獨立的「直立柱區塊」，柱子高度用滿車廂高度（`layers` 層），需要幾柱由「箱數 ÷ 每柱層數」無條件進位決定，再把每種產品的柱子佔用面積加總，跟車廂地板總面積比較。這個方法**不會**去模擬不同形狀的箱子互相卡榫、填補畸零空間的狀況，因此：

- 若估算結果是「放得下」，實務上通常真的放得下（因為地板面積是必要條件、且以整柱方式計算相對保守）。
- 若估算結果卡在臨界值（利用率接近 100%），仍建議實際試裝或找排版軟體驗證。

### 4.5 貨車推薦排序邏輯（功能 03）

```
對公司內每一台貨車都跑一次 4.4 的檢查
fitting = 篩選出 fits === true 的車輛，依 ratio 由高到低排序（利用率越高排越前面，代表越「剛好」不浪費空間）
notFitting = fits === false 的車輛，同樣依 ratio 排序（用來在「完全沒有車能裝」時，列出最接近的幾台參考車輛，目前原型抓前 3 台）
```

---

## 5. 功能模組規格

### 5.1 功能 01：單一產品容量查詢

**輸入**
- 下拉選擇一台貨車（`car_number`）
- 下拉選擇一種包裝箱（`box_name_id`，選項顯示尺寸與 `每箱 N pcs`）

**輸出**
- 底層排列：`rows × cols = footprint`（每層箱數）
- 堆疊層數：`layers`
- 總箱數：`total_boxes`
- 每箱包裝數量、總數量（pcs）
- 俯視圖（依實際比例畫出車廂地板 + 箱子網格，斜線區塊代表放不下的剩餘空間）
- 側視圖（車廂高度 + 堆疊層數量的長條圖）
- 放不下時：顯示「放不下」＋明確原因，不顯示 0 箱與示意圖

**儲存 / 清單模式**
- 「儲存這筆結果」：把這筆查詢結果（貨車、箱型、排列、層數、總箱數、總數量、儲存時間）存成一筆紀錄。
- 「切換到清單模式」：另開一個表格檢視，列出所有存過的紀錄，可逐筆刪除、可整批清空。
- 原型使用瀏覽器端 `window.storage` 這個 key-value 儲存（僅限 claude.ai 預覽環境有效；若下載後直接用瀏覽器開啟，會退回成「本次分頁記憶體暫存，重新整理就消失」）。
  **正式版建議**：改成寫入真正的資料庫資料表（例如新增 `zz_load_capacity_log`），欄位比照下方 5.4 匯出欄位設計，並加上 `created_by`、`created_at`，才能跨裝置、跨使用者永久保存。

**匯出 Excel**
- 「匯出 Excel」：匯出目前這一筆查詢結果（單一 sheet，單一列）。
- 清單模式下「匯出全部紀錄 Excel」：把所有已儲存的紀錄一次匯出成一個 sheet，每筆紀錄一列。

### 5.2 功能 02：混裝裝載確認

**輸入**
- 下拉選擇一台貨車
- 動態新增／移除多列「產品＋數量」（數量單位＝箱數）

**輸出**
- 放得下／放不下（估算）的判定橫幅
- 地板面積需求 / 總面積、利用率百分比、利用率長條圖
- 共 X 箱、Y pcs（依 `packing_qty` 換算的總件數）
- 每項產品明細表：數量(箱)、單柱可疊層數、需要柱數、佔用面積、總數量(pcs)；若該產品本身放不進這台車（不論數量），直接顯示原因（長寬排不下 / 高度超過）

**匯出 Excel**
- 「匯出此車 Excel」：兩個 sheet
  - `總覽`：貨車尺寸、地板總面積、使用面積合計、地板利用率%、是否放得下、總箱數、總數量(pcs)
  - `產品明細`：每項產品一列（貨車、車廂長寬高、產品、數量(箱)、每箱包裝數量、總數量(pcs)、單柱可疊層數、需要柱數、佔用面積、備註）

### 5.3 功能 03：貨車推薦

**輸入**
- 動態新增／移除多列「產品＋數量」（不需指定貨車）

**輸出**
- 對公司所有貨車（已過濾掉尺寸不完整的）逐一檢查
- 若有放得下的車：列出所有放得下的車輛卡片，依地板利用率高到低排序
- 若沒有任何車放得下：顯示「沒有任何一台貨車放得下這批貨」，並列出最接近的 3 台車輛卡片供參考
- 每張車輛卡片顯示：車號、地板利用率%、車廂尺寸、共幾箱幾 pcs、利用率長條圖、每項產品的柱數/層數明細

**匯出 Excel**
- 每張車輛卡片都有「匯出此車 Excel」（格式同 5.2 的兩個 sheet）
- 「匯出全部貨車 Excel（整列總覽）」：**一個 sheet**，把「所有被檢查過的貨車」（不限於畫面上顯示的卡片）各佔一列，欄位：貨車、車廂長寬高、地板總面積、使用面積合計、地板利用率%、是否放得下、總箱數、總數量(pcs)

---

## 6. Excel 匯出技術規格

原型使用 [SheetJS (xlsx)](https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js) 純前端產生 `.xlsx`，不經過後端。正式版可以：

- 前端仍用 SheetJS／`exceljs` 產生，或
- 改成後端 API 產生（例如 Node.js 用 `exceljs`、Python 用 `openpyxl`），前端呼叫 API 下載檔案 — 這樣做的好處是可以統一套用公司的 Excel 樣式（字型、公司抬頭、凍結窗格等）。

檔名慣例（可依公司命名規則調整）：
- `單一產品容量_{車號}_{箱型}.xlsx`
- `單一產品容量_全部紀錄.xlsx`
- `混裝裝載確認_{車號}.xlsx`
- `貨車推薦_全部貨車.xlsx`

---

## 7. 前端畫面設計參考（Design Tokens）

原型採用「工業藍圖」風格，正式版若沿用可參考下列色票／字體：

| 用途 | 色碼 |
|---|---|
| 主色（深藍，標題／邊框） | `#1b2a3d` |
| 次要文字（藍灰） | `#3d5066` |
| 底色（圖紙米白） | `#f5f3ee` |
| 格線（淺卡其） | `#dcd7c9` / `#c9c2ad` |
| 強調色（安全橘，主要按鈕／重點數字） | `#e8703a` |
| 強調色淺底 | `#f6d9c8` |
| 輔助色（暗青） | `#3d6b6b` |
| 成功／放得下 | 底 `#e7f0e7`　字 `#2c5a2c` |
| 失敗／放不下 | 底 `#f7e3dd`　字 `#a13c1c` |

字體：標籤與內文用 `Inter`（或 `Noto Sans TC`），數字／代碼類（尺寸、公式、車號箱號）一律用等寬字 `IBM Plex Mono`，強化「這是可信賴的規格數據」的視覺語感。

版面：左側固定連線／篩選面板（narrow sidebar），右側主要內容用分頁（tab）切換三個功能，避免三個功能互相干擾版面。結果區一律搭配：判定橫幅（verdict banner，顏色區分放得下/放不下）→ 利用率長條圖 → 明細表格 → 圖示（俯視圖／側視圖，僅功能 01 有）。

---

## 8. 已知限制與後續可優化方向

1. **混裝排版是估算值，非精確排版**。若正式上線後常常卡在臨界值誤判，建議導入專門的 2D/3D bin-packing 演算法（例如 guillotine cut 或 skyline 演算法），或至少把「柱狀估算法」的保守係數做參數化，方便日後校正。
2. **儲存機制需要正式化**。原型的「儲存」只是示範用的 key-value 暫存，正式版務必落地到資料庫並綁定使用者身份，才能做到跨裝置、可稽核。
3. **Excel 樣式偏簡單**（目前是純資料表，沒有公司抬頭、沒有凍結窗格、沒有条件式格式）。正式版建議統一制定一份公司報表樣式範本。
4. **沒有處理「同一產品拆成多個批次」或「棧板／墊高」等實務上常見的裝載細節**，目前只單純比較箱子本身尺寸與車廂尺寸。
5. **資料過濾目前在前端做**，建議移到後端／資料庫查詢層，減少不必要的資料傳輸並確保各端（App、報表、其他系統）看到的「有效貨車清單」邏輯一致。

---

## 9. 附錄：目前原型檔案

原型檔名：`truck-load-calculator.html`（單一 HTML 檔，內嵌 CSS/JS，直接用瀏覽器開啟即可運作，Supabase 連線資訊由使用者在畫面上輸入 Project URL 與 anon key，透過 REST API `fetch` 讀取兩張資料表）。

本文件所描述的欄位名稱、公式、功能規格、匯出格式，均以此檔案的最終版本為準，可作為重新開發（例如遷移到 Next.js + 正式後端 API + 正式資料庫紀錄表）時的規格依據。
